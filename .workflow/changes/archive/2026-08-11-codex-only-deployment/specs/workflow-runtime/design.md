# workflow-runtime Delta Design

Shared workflow rules remain empty by default. Downstream repositories may add their own `.agents/rules`, skills, and guidance outside managed blocks without turning those assets into shared deployment input.
