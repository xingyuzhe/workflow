# Debug Gate (MUST on failures)

On errors, test failures, or unexpected behavior:

1. Reproduce
2. Form a hypothesis
3. Validate with a minimal check
4. Then fix

Do **not** apply random speculative fixes.

After **three** failed fix attempts on the same issue: pause, report findings, and suggest updating OpenSpec artifacts if the issue looks design-level.
