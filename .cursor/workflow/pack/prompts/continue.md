# Continue change

Advance the next incomplete artifact for an existing change.

1. `openspec status --change "<name>"`
2. `openspec instructions <artifact> --change "<name>"`
3. Write the artifact. Specs phase: create `spec.md`+`design.md` pairs.
4. Respect SSOT in `openspec/config.yaml`.
