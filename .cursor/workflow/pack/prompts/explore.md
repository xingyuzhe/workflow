# Explore

Think through ideas before or during a change. Do not implement unless the user explicitly asks.

Use OpenSpec CLI as needed (`openspec list`, `openspec status`). Prefer questions and options over committing to a design prematurely.
