# Fast-forward change

Create all artifacts needed for apply in one pass (proposal → specs pairs → design → tasks).

1. Follow `branch.md`.
2. Use schema **workflow-spec**.
3. Every capability: `spec.md` + `design.md` together.
4. Stop when apply-ready; suggest `/opsx:apply` or grill first.
