# Archive change

1. Confirm tasks complete and verify done (or user accepts residual risk).
2. Archive via OpenSpec CLI; keep main specs paired (`spec.md`+`design.md`).
3. Then load `finish.md` for merge/PR/discard options. Wait for user choice.
