# deploy-kit Delta Design

The deployment module resolves client names once and passes the normalized set through adapter generation, cleanup, metadata, and validation. Shared core installation is independent of this selection. Installed metadata supplies the default scope for subsequent Doctor and repair operations.
